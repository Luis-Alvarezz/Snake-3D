import { LifeCycle } from "../types/helpers";

export default class Food implements LifeCycle {
    public start(): void {

    }

    public update(): void {
        
    }

    public dispose(): void {
        
    }
}