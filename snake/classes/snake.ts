import { LifeCycle } from "../types/helpers";

export default class Snake implements LifeCycle {
    public start(): void {

    }

    public update(): void {
        
    }

    public dispose(): void {
        
    }
}