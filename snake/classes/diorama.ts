import { LifeCycle } from "../types/helpers";

export default class Diorama implements LifeCycle {
    public start(): void {

    }

    public update(): void {
        
    }

    public dispose(): void {
        
    }
}